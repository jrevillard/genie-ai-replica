# Core service
fastapi==0.110.0
uvicorn==0.29.0
python-multipart==0.0.6

# LLM / agent
# google-genai 0.8.x has a field typed as the built-in `any` function instead of
# typing.Any, which causes pydantic >=2.11 to raise ArbitraryTypeWarning during
# schema generation (and a hard error in stricter warning modes). The 1.x line
# fixes that annotation. Pin <2.0.0 to stay within the stable 1.x API surface.
google-genai>=1.0.0,<2.0.0

# MCP protocol
mcp>=1.0.0

# Geo / spatial
shapely==2.0.3
pyproj==3.6.1
geojson==3.1.0

# Web scraping (BMD BAMIS)
requests==2.31.0
beautifulsoup4==4.12.3

# Schema validation
pydantic>=2.11.0

# Early warning system — storage
python-arango==7.9.1

# Early warning system — scheduler
APScheduler==3.10.4

# Optional — Twilio SMS / voice (install if TWILIO_ACCOUNT_SID is set):
# twilio==9.0.0
