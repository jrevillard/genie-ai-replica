# generated: 2026-08-17
# project: genie-ai
# project_key: server-side-tools
# tracking_system: gitlab-issues (fallback: file-system)
# story_location: _bmad-output/implementation-artifacts
# prd: _bmad-output/planning-artifacts/prds/prd-server-side-tools.md
# epics: _bmad-output/planning-artifacts/epics.md
# architecture: _bmad-output/planning-artifacts/architecture.md
# branch: feat/sst

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#   - blocked: Cannot start — see the inline reason
#   - deferred: Deliberately out of this initiative's scope
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed

generated: 2026-08-17
project: genie-ai
project_key: server-side-tools
tracking_system: gitlab-issues
story_location: _bmad-output/implementation-artifacts

# TRACKING NOTE
# =============
# GitLab Issues are the primary source of truth when reachable (project-context.md).
# `glab` is NOT installed on the current machine, so this file is the working source
# of truth until the CLI is installed or the re-baselining is done in the GitLab UI.
#
# Re-baselining #696–#725 (epics.md, final section):
#   - original Epic 1 issues (registry / executor / mcpo / D14 / D15 / D17)
#       → close as "subsumed by LangGraph in the workflows service"
#   - original Epic 2/3/4 issues → retain, re-point at the stories below,
#       add Epic 1 governance as a cross-cutting dependency on every tool-call issue
#   - labels: prd::server-side-tools, server-side-tools::epic-N, status::*, type::*

# EXTERNAL PREREQUISITE
# =====================
# The OPEA 1.5 overlay bump (decision doc Part A, ~3–5 engineer-days, jrevillard's
# modules) is NOT part of this initiative. It is a prerequisite for exactly one story
# (1-7), which needs `ServiceType.WORKFLOW = 101` from bump task A1. Every other story
# is bump-independent and can start immediately.

# WORKTREE STRATEGY
# ==================
# Stories are grouped into worktrees by file overlap to avoid merge conflicts.
# Naming convention: .worktrees/<worktree-name> → branch feature/<worktree-name>
# Stories in the SAME worktree run sequentially. Different worktrees run in parallel.
#
# ─── Epic 1 — Governance & tool host (2 worktrees) ───
#   sst-governance : 1-1 → 1-2 → 1-3 → 1-4 → 1-5   (all genie-ai-overlay/tools/)
#                    1-2 and 1-3 are mutually independent — split them out if capacity allows
#   sst-tools-infra: 1-6 → 1-7                      (compose / env / env.j2 / Dockerfile)
#
# ─── Epic 2 — Web search (2 worktrees, after 1-4 merges) ───
#   sst-websearch-py: 2-1 → 2-2 → 2-3 → 2-4 → 2-5 → 2-6 → 2-7
#                     2-6 touches genieai_chatqna.py — coordinate with jrevillard
#   sst-websearch-ui: 2-8 → (2-9 ‖ 2-10)
#                     2-8 is independent of the Python track and BLOCKS 2-9 + 2-10.
#                     Start it early — nothing renders until it lands.
#
# ─── Epic 3 — Stream ingestor (2 worktrees; may run parallel to Epic 4) ───
#   sst-ingestor-dataprep: 3-1 → 3-2                (dataprep — jrevillard's module)
#   sst-ingestor-svc     : 3-3 → 3-4 → 3-5 → 3-6 → 3-7 → 3-8 → 3-9 → 3-10 → 3-11
#                          cross-worktree: 3-6 waits on 3-1; 3-11 waits on 3-2 + 3-6
#
# ─── Epic 4 — Admin surface (2 worktrees, after 1-4 merges) ───
#   sst-admin-api: 4-1 → 4-2 → 4-10
#   sst-admin-ui : 4-3 → 4-4 → 4-5 → 4-6 → 4-7 → 4-8 → 4-9   (all AdminDashboard/admin/Tools)
#                  cross-worktree: 4-3 waits on 4-2; 4-5 waits on 3-8; 4-6/4-7 wait on 1-3

development_status:
  # ─────────────── Epic 1 — Governance & tool host ───────────────
  # Hard gate for #603. Stories 1-1..1-6 need no running service and no bump.
  epic-1: backlog
  1-1-declared-schemas-package: backlog # worktree: sst-governance
  1-2-pii-redactor-abc-and-implementations: backlog # worktree: sst-governance — NFR6 gating suite
  1-3-redis-primitives-rate-limiter-circuit-breaker-audit-stream: backlog # worktree: sst-governance — all greenfield (E5)
  1-4-governance-pipeline: backlog # worktree: sst-governance — depends 1-2, 1-3
  1-5-otel-spans-on-governance-phases: backlog # worktree: sst-governance — depends 1-4
  1-6-presidio-deployment-and-config-plumbing: backlog # worktree: sst-tools-infra
  1-7-minimum-tool-host-shell: blocked # BUMP-GATED: needs ServiceType.WORKFLOW=101 (bump task A1)
  epic-1-retrospective: optional

  # ─────────────── Epic 2 — Web search (SearXNG) ───────────────
  # Second hard gate for #603. Depends on Epic 1 stories 1-1..1-4.
  epic-2: backlog
  2-1-searxng-deployment: backlog # worktree: sst-websearch-py — AGPL sign-off gates production (OQ-SST-5)
  2-2-pluggable-search-backend-and-searxng-client: backlog # worktree: sst-websearch-py
  2-3-domain-whitelist-enforced-at-executor: backlog # worktree: sst-websearch-py — NFR11, not bypassable
  2-4-triggers-low-confidence-time-sensitive-llm-fallback: backlog # worktree: sst-websearch-py
  2-5-fusion-budget-quality-threshold: backlog # worktree: sst-websearch-py
  2-6-chatqna-integration-at-both-seams: backlog # worktree: sst-websearch-py — BOTH seams (E6); coordinate with jrevillard
  2-7-degradation-and-transparent-insufficiency: backlog # worktree: sst-websearch-py — NFR12 zero fabrication
  2-8-sse-metadata-contract-extension: backlog # worktree: sst-websearch-ui — BLOCKS 2-9 + 2-10 (E3); start early
  2-9-vue-citation-and-degradation-rendering: backlog # worktree: sst-websearch-ui — depends 2-8
  2-10-flutter-citation-and-degradation-parity: backlog # worktree: sst-websearch-ui — depends 2-8
  epic-2-retrospective: optional

  # ─────────────── Epic 3 — Stream ingestor ───────────────
  # No #603 dependency. May run parallel to Epic 4.
  epic-3: backlog
  3-1-chunk-source-type-and-additive-chunk-metadata: backlog # worktree: sst-ingestor-dataprep — needs OQ-SST-6 (OKF enum)
  3-2-retract-expired-feed-chunks: backlog # worktree: sst-ingestor-dataprep — new method, not new class
  3-3-rss-atom-polling: backlog # worktree: sst-ingestor-svc
  3-4-json-api-polling-with-field-mapping: backlog # worktree: sst-ingestor-svc
  3-5-webhook-push-ingestion: backlog # worktree: sst-ingestor-svc — 401 unauth, 429 + Retry-After
  3-6-route-through-existing-tei-and-shared-arangodb: backlog # worktree: sst-ingestor-svc — waits on 3-1
  3-7-per-feed-lifecycle-retention-ttl-update-vs-append: backlog # worktree: sst-ingestor-svc
  3-8-feed-definition-persistence: backlog # worktree: sst-ingestor-svc — needs OQ-SST-1
  3-9-per-feed-resilience-breaker-dlq-backoff: backlog # worktree: sst-ingestor-svc
  3-10-health-endpoints-and-per-feed-health: backlog # worktree: sst-ingestor-svc
  3-11-regression-guard: backlog # worktree: sst-ingestor-svc — PRODUCTION GATE; needs OQ-SST-4
  epic-3-retrospective: optional

  # ─────────────── Epic 4 — Admin surface ───────────────
  epic-4: backlog
  4-1-two-keycloak-roles-and-require-role: backlog # worktree: sst-admin-api — requireAdmin is single-role today
  4-2-api-tools-bff-proxy: backlog # worktree: sst-admin-api — THREE registries (E7): ROUTE_CONFIGS, Kong, env.j2
  4-3-admin-tools-tab: backlog # worktree: sst-admin-ui — QueryInspector pattern, NOT the 4446-line monolith (E1)
  4-4-tool-management-and-domain-whitelist-editor: backlog # worktree: sst-admin-ui
  4-5-feed-management-form: backlog # worktree: sst-admin-ui — waits on 3-8
  4-6-audit-log-viewer: backlog # worktree: sst-admin-ui — FOI access path (NFR8)
  4-7-health-overview: backlog # worktree: sst-admin-ui
  4-8-list-and-grant-user-capabilities: backlog # worktree: sst-admin-ui — reuse searchUsers; grant half is net-new (E2)
  4-9-i18n-across-all-14-locales: backlog # worktree: sst-admin-ui — CI GATE: localeConsistency.test.js
  4-10-ansible-tags-tools-and-profile-tools: backlog # worktree: sst-admin-api — no `tools` tag exists today
  epic-4-retrospective: optional

  # ─────────────── Post-MVP (non-blocking) ───────────────
  5-1-tool-invocation-analytics-dashboard: deferred # Growth feature; consumes the audit stream, blocks nothing

# OPEN DECISIONS (block the stories noted above)
# ==============================================
#   OQ-SST-1  feed-config persistence: registry (dropped) or ingestor config (keep)?  → 3-8
#   OQ-SST-2  original 17-ADR spec: leave on feat/server-side-tools/prd or port?      → re-baselining
#   OQ-SST-3  confirm governance + web search are hard blockers for #603              → epic sequencing
#   OQ-SST-4  which curated-only baseline validates feed-chunk relevance?             → 3-11
#   OQ-SST-5  SearXNG AGPL sign-off recorded?                                         → 2-1 to production
#   OQ-SST-6  ChunkSourceType enum ownership with OKF                                 → 3-1
#   OQ-SST-7  SSE metadata: ad-hoc fields or declared contract?                       → 2-8
#   OQ-SST-8  tool-host boundary: shell only vs. more                                 → 1-7
