# Test-only dependencies for translation_v2.
# Kept separate from haystack-chatqna/requirements.txt so Step 1 does
# not edit any existing config file. Install via:
#   python -m pip install -r src/translation_v2/requirements-test.txt
pytest>=7.4,<9.0
pytest-asyncio>=0.23,<1.0
# Step 4 — PDF ingest + retrieval
pymupdf>=1.24,<2.0
# rank_bm25, numpy, scikit-learn, httpx already satisfied by the shared venv.
