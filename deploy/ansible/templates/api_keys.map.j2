# GPU Node API Keys (auto-generated by Ansible)
# One key per GENIE.AI client — shared across all 5 GPU services
# To add/remove keys, edit gpu_api_keys in group_vars/gpu.vault
{% for key_entry in gpu_api_keys %}
{{ key_entry.key }} 1;
{% endfor %}
