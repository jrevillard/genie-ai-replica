name: genie_ai_mobile
description: "A new Flutter project."

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Core networking & utilities
  http: ^1.6.0
  crypto: ^3.0.3
  shared_preferences: ^2.2.2

  # Markdown rendering for chatbot messages
  flutter_markdown: ^0.7.3

  # PDF generation and printing/export
  pdf: ^3.11.1
  printing: ^5.13.1

  # File system access for saving PDF
  path_provider: ^2.1.4

  # URL launching for opening documents in right sidebar
  url_launcher: ^6.3.1

  cached_network_image: ^3.3.1

  flutter_svg: ^2.0.10+1
  # Profile icon upload from gallery/camera
  image_picker: ^1.1.2     

  # Document uploads (birth cert, ID, etc.)
  file_picker: ^8.1.2      

  # Country picker 
  country_picker: ^2.0.26

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Enable generation of localized Strings from arb files.
  generate: true

  assets:
    - assets/images/
    - assets/config/
    - assets/config/quickhelp/
    - assets/FAQ.md
    - assets/icons/
    - assets/i18n/en.json
  fonts:
    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto-Regular.ttf
