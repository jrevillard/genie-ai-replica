# PII key list — single source of truth
# Referenced by both Node SDK redactor and collector transform.
# Any change requires CODEOWNER @jrevillard approval.

password, api_key, api-key, apiKey, apikey,
session_id, session-id, sessionId,
user_id, user-id, userId,
email, mail,
auth_token, auth-token, authToken, bearer_token, bearerToken, authorization,
token, secret,
api_secret, api-secret, apiSecret,
credential, credential_id, credentials,
openai_api_key, anthropic_api_key,
private_key, secret_value,
user_query, user-query,
llm_response, llm-response,
document_text, document-text,
