# BMM Module Configuration
# Generated by BMAD installer
# Version: 6.0.4
# Date: 2026-03-30T12:13:58.632Z

project_name: genie-ai
user_skill_level: intermediate
planning_artifacts: "{project-root}/_bmad-output/planning-artifacts"
implementation_artifacts: "{project-root}/_bmad-output/implementation-artifacts"
project_knowledge: "{project-root}/docs"

# Core Configuration Values
user_name: Jerome
communication_language: French
document_output_language: English
output_folder: "{project-root}/_bmad-output"

# GitLab Tracking Configuration
# When enabled, sprint tracking is mirrored to GitLab Issues via glab CLI.
# Fallback to file-system tracking if GitLab is unavailable.
gitlab_tracking:
  enabled: true
  # PRD key used for label scoping (e.g., "keycloak-idp" creates prd::keycloak-idp label)
  prd_key: "keycloak-idp"
  # Optional: link to an existing GitLab issue as PRD parent (skip auto-creation)
  # prd_parent_issue: 218
  # Custom task for sync operations
  sync_task: "{project-root}/_bmad/_config/custom/sync-gitlab-issues.md"
