// E2E test secrets — copy to e2e_secrets.dart and fill in values.
// This file is gitignored. Never commit real secrets.
class E2eSecrets {
  const E2eSecrets({required this.keycloakAdminPassword});

  final String keycloakAdminPassword;
}

const e2eSecrets = E2eSecrets(
  keycloakAdminPassword: 'YOUR_KEYCLOAK_ADMIN_PASSWORD',
);
