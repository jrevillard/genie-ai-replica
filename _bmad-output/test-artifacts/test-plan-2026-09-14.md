# admin-logs-victorialogs PRD — Local Verification Test Plan

**Date**: 2026-09-14
**Branch**: feat/admin-logs-victorialogs/prd-fix-shared-lib
**Stack**: local docker compose (project `admin-logs-prd`) + remote GPU node (10.0.0.110, off-stack for chat)
**Scope**: 7 epics × 45 stories; 6 PRD success criteria + 5 rollback switches

## Stack status at start

- 23/27 services Up (healthy): backend, doc-repo, keycloak, frontend, nginx, kong, postgres, redis, arango, clamav, otel-collector, victorialogs, victoriametrics, victoriatraces, grafana, tempo-proxy, plus 8 OPEA orchestrators (chatqna-xeon-backend-server, embedding, reranker, retriever-arango, dataprep-arango, textgen)
- 4 OPEA sidecars (`restart: "no"`, one-shot by design): translation, guardrail, chatqna-xeon-nginx-server, chatqna-xeon-ui-server
- Fix applied during prep: `opea/translation:1.5` → `1.3` (Docker Hub max tag)

## Phase A — Smoke (no functional verification yet, just liveness)

| # | Check | Method | Pass criterion |
|---|---|---|---|
| A.1 | Backend healthy | `docker inspect backend` | status=healthy, no crash loop |
| A.2 | Doc-repo healthy | `docker inspect doc-repo` | status=healthy, no MODULE_NOT_FOUND |
| A.3 | VL receiving logs | query `victorialogs:9428/select/logsql/query` | ≥1 row per service in last 1m |
| A.4 | OTel collector up | `docker ps` | Up, fluentd driver attached |
| A.5 | D1 enforced (no profile gating) | grep compose | VL + OTel-collector have NO `profiles:` key |

## Phase B — PRD success criteria

### B.1 Winston JSON via VL transport; trace_id/span_id are JSON fields

PRD AC #3: "Winston emits JSON via VL transport; trace_id/span_id are fields, not printf substrings"

- B.1.1 Query VL for recent backend logs, verify each has `trace_id` and `span_id` as TOP-LEVEL JSON fields (not buried in `_msg` string).
- B.1.2 Verify shared/lib/victorialogs-transport.js is loaded (process env `LOG_TO_VICTORIALOGS=1`).
- B.1.3 Compare to file-transport JSON (should be IDENTICAL shape, story 2.5).

### B.2 Admin `GET /api/admin/logs` returns schema-identical JSON via VL

PRD AC #1: schema-identical JSON via VL

- B.2.1 Get Keycloak realm token (per SERVER-TESTING.md, enable ROPC temporarily then revert).
- B.2.2 Hit `/api/admin/logs` with `ADMIN_LOGS_SOURCE=victorialogs` → verify JSON shape matches pre-merge baseline.
- B.2.3 Hit `/api/admin/logs` with `ADMIN_LOGS_SOURCE=file` → verify same shape (D2 escape hatch).

### B.3 `POST /api/admin/security-scan` completes <2s, same shape

PRD AC #2

- B.3.1 Time security-scan POST under VL backend.
- B.3.2 Verify response shape: `{vulnerabilities: {critical, medium, low: [...]}}`.

### B.4 F4 regex at admin-dashboard-service.js:525 removed

PRD AC #4

- B.4.1 grep F4-fix marker in admin-dashboard-service.js.

### B.5 VL + OTel Collector always run, no profile gating (D1)

PRD AC #5 + D1 decision

- B.5.1 `docker compose --profile observability config --services` | grep vl/otel → expect NO match (always-on, not profile-gated).
- B.5.2 Bring down observability profile stack and verify VL/otel still run.

### B.6 All 5 rollback switches tested

Per PRD §Success criteria

- B.6.1 `ADMIN_LOGS_SOURCE=file` → `/api/admin/logs` falls back to file-based.
- B.6.2 `LOG_TO_VICTORIALOGS=0` → backend logs skip VL transport (file-only, console-only).
- B.6.3 `VL_FAIL_OPEN=true` → VL outage degrades gracefully (rate-limit 1/min).
- B.6.4 `LOG_TO_FILE=1` → file transports re-enable.
- B.6.5 `SECURITY_SCAN_BACKEND=file` → security-scan falls back to file-based.

## Phase C — Per-epic functional

### Epic 1 (Pipeline enablement, D1)
- 1.1 VL + otel-collector always-on (covered by A.5 + B.5)
- 1.2 OTel Collector config has OTLP logs receiver — verify config YAML

### Epic 2 (Winston → VL JSON + OTel)
- 2.1 OTel deps in shared/lib + backend
- 2.2 boolean-env.js + otel-batch-config.js helpers present
- 2.4 victorialogs-transport.js present
- 2.5 logger.js emits JSON (covered B.1)
- 2.6 tracing.js setGlobalLoggerProvider
- 2.8/2.9 PII scrubbing test
- 2.11 VL integration test (fake OTLPLogExporter)
- 2.12 log_record_dropped_total counter

### Epic 3 (doc-repo producer-side)
- 3.1 doc-repo OTel deps
- 3.2 doc-repo tracing.js logs-only path
- 3.4 ClamAV observability events

### Epic 4 (MELT seam)
- 4.1 melt/types.js LogQuery types
- 4.2 melt/index.js exports
- 4.3 melt/victorialogs-client.js axios wire + normalize
- 4.4 shared/lib/index.js re-exports
- 4.5/4.6 VL client + config validator tests

### Epic 5 (Logs facility rewire + F4 fix)
- 5.1 NDJSON fixture
- 5.2 ingestion script
- 5.3 logs-service.js rewrite
- 5.4 admin-dashboard-service.js drops fs.readFile
- 5.7 LogSearchDialog.vue computed banner
- 5.8 contract test (file path vs VL path)
- 5.9 degradation test (5xx/ECONNREFUSED)
- 5.10 admin-source test (env toggle)
- 5.11 LogSearchDialog test
- 5.12 grep fix

### Epic 6 (Security scanner rewire)
- 6.1 security-scan-service drops worker_threads
- 6.2 VL bulk queries + dedupe
- 6.4 file fallback works

### Epic 7 (Cleanup)
- 7.1 logger.js LOG_TO_FILE guard
- 7.2 routes return `{deprecated:true,...}`
- 7.3 shared/lib/index.js re-exports
- 7.4 ESLint no-restricted-imports for shared/lib

## Phase D — Findings report

Aggregate pass/fail per phase + per epic. Catalog any:
- AC gaps
- Runtime blockers (the prior agent caught one — the shared-lib path bug; verify fully gone)
- Rollback switch regressions

## Findings template

```markdown
| Phase | Test | Status | Evidence |
|---|---|---|---|
```

