# GENIE.AI Docker Swarm - Cloud Deployment (bb-ai-vm-02)
# App node: 10.0.0.101 (remote GPU at ai.assembly.govstack.global:444)

[cloud_deploy]
genieai-cloud ansible_host=10.0.0.101

[swarm_managers]
genieai-cloud

# gpu_nodes intentionally empty — GPU models run on the remote GPU node

[all:vars]
ansible_user=govstack
