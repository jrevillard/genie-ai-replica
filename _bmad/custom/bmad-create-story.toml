# Issue Tracking override for bmad-create-story (legacy shim, deprecated upstream).
# Delegates to common/post-dev-complete-create-story.yaml.
# Superseded by bmad-build + bmad-build-auto overrides — kept for retrocompat.

[workflow]

activation_steps_append = [
  "Read `_bmad/_config/custom/bmad-workflow-lang.md` for the workflow language specification, then execute `_bmad/_config/custom/workflows/create-story/activation.yaml`.",
]

on_complete = """
Read `_bmad/_config/custom/bmad-workflow-lang.md` for the workflow language specification, then execute `_bmad/_config/custom/workflows/common/post-dev-complete-create-story.yaml`.
"""