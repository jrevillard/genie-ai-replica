# Prose Editorial Review — PRD: OPEA 1.3 → 1.5 Upgrade

**Scope:** `prd.md` + `addendum.md`
**Reader type:** humans (platform engineers, PM, downstream BMAD workflows)
**Baseline:** Microsoft Writing Style Guide
**Mode:** conservative polish pass — content is sacrosanct; only genuine communication issues (clarity, ambiguity, precision, readability) are flagged. The `addendum.md` was reviewed and needs no prose changes.

| Original Text | Revised Text | Changes |
|---------------|--------------|---------|
| That pin has now drifted **7.5 months** behind the latest stable release, **v1.5** (2025-12-22) — still the newest OPEA release as of 2026-08-07. | Consider: "That pin has now drifted behind the latest stable release, **v1.5** (2025-12-22) — still the newest OPEA release as of 2026-08-07 (7.5 months after it shipped)." — and confirm which quantity "7.5 months" is meant to express. | Precision/ambiguity (query): "7.5 months" currently modifies "drifted behind", but 7.5 months equals the time since v1.5 shipped (2025-12-22 → 2026-08-07), not the v1.3→v1.5 release gap (~7.2 months). The addendum uses the same figure for the former quantity. Attach the number to the clause that states the intended fact. |
| v1.5 base signatures are `(self, inputs, *args, **kwargs)`-style, compatible with GENIE's positional override, **but** behavioral (not just import) — see below | v1.5 base signatures are `(self, inputs, *args, **kwargs)`-style, compatible with GENIE's positional override, **but** this surface needs behavioral verification, not just an import check — see below | Clarity: "behavioral (not just import)" is a sentence fragment; adding the predicate "needs behavioral verification" makes the contrast with "compatible" explicit. |
| (8) the dataprep `_parent_mod.ARANGO_DB_NAME` monkeypatch target — if v1.5 moved where the constant lives the patch **silently no-ops**; | (8) the dataprep `_parent_mod.ARANGO_DB_NAME` monkeypatch target — if v1.5 moved where the constant lives, the patch **silently no-ops**; | Readability: missing comma after "lives" creates a garden path ("the constant lives the patch"); the comma separates the conditional clause from its consequence. |
| The **`schedule()` kwargs-forwarding contract is proven first**, as a blocking pre-rebase spike on a bare v1.5 clone with no overlay … The remaining surfaces: (2) … | Consider: label the first surface "(1)" so the enumeration reads (1)–(10) rather than jumping from an unnumbered first item to "(2)". | Consistency (query/optional, structural): the first coupling surface is described in prose but unnumbered, while the following nine are numbered (2)–(10). Adding "(1)" makes the enumeration coherent. |
| These gates gate the merge to `main`. | These gates must pass before the merge to `main`. | Flow: the noun "gates" is immediately followed by the verb "gate" — an awkward repetition that slows reading; rephrased to state the precondition without the wordplay. |
