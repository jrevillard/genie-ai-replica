---
title: 'Bump langchain-arangodb to >=1.2.0 and add contract test for label-filter exclusion'
type: 'chore'
created: '2026-08-13'
status: 'done'
baseline_revision: 'e3696035b5bea781d8550c7b7857d67c8f5b9181'
review_loop_iteration: 0
followup_review_recommended: false
context: []
warnings: []
deferred: []
---

<intent-contract>

## Intent

**Problem:** The retriever's langchain-arangodb dependency is pinned at 0.0.6 in requirements.in and requirements-cpu.txt. The intent is to restore the >=1.2.0 filter_clause behavioral fix-pin that was deferred during the v1.5 lock compilation, and add a contract test that validates the label-filter exclusion behavior.

**Approach:** Bump langchain-arangodb from 0.0.6 to >=1.2.0 in requirements.in, regenerate requirements-cpu.txt with pip-compile, and update the existing contract test's version assertion to validate >=1.2.0 instead of ==0.0.6. The label-filter exclusion contract test already exists in test_contract_label_filter.py.

## Boundaries & Constraints

**Always:** Preserve compiled-lock fidelity with upstream OPEA v1.5 where pins are explicitly marked as v1.5-aligned. Update comments to reflect the new version rationale.

**Block If:** The pip-compile fails due to dependency conflicts with langchain-core or other langchain-* packages.

**Never:** Modify the label-filter logic in the retriever code. Change any other dependency pins unless explicitly required by the bump.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| HAPPY_PATH | requirements.in with langchain-arangodb>=1.2.0 | requirements-cpu.txt reflects >=1.2.0 with resolved dependencies | No error expected |
| VERSION_ASSERTION | langchain-arangodb>=1.2.0 installed, contract test runs | test_installed_arangovector_exposes_filter_clause_named_param passes | Version mismatch fails test |
| FILTER_CLAUSE_NAMED_PARAM | langchain-arangodb>=1.2.0 installed | ArangoVector methods expose filter_clause as named parameter | Missing param fails test |

</intent-contract>

## Code Map

- `genie-ai-overlay/retriever/requirements.in` -- source pin file for retriever dependencies. Line 28-37 contains langchain-arangodb pin at ==0.0.6 with explanatory comment.
- `genie-ai-overlay/retriever/requirements-cpu.txt` -- compiled lock file generated by pip-compile. Line 1913 contains langchain-arangodb==0.0.6.
- `genie-ai-overlay/contracts/test_contract_label_filter.py` -- contract test for label-filter exclusion. Line 150-152 asserts version ==0.0.6, needs update to >=1.2.0.
- `genie-ai-overlay/retriever/genieai_retriever_arangodb.py` -- retriever implementation (read-only, no changes needed). Uses _build_aql_filter_clause and passes filter_clause to ArangoVector methods.

## Tasks & Acceptance

**Execution:**
- `genie-ai-overlay/retriever/requirements.in` -- Update langchain-arangodb pin from ==0.0.6 to >=1.2.0 at line 37. Update comment at lines 28-36 to reflect that the bump restores the filter_clause behavioral fix-pin that was deferred during v1.5 lock compilation.
- `genie-ai-overlay/retriever/requirements-cpu.txt` -- Regenerate compiled lock with pip-compile: `uv pip compile --generate-hashes --python-version 3.11 --python-platform x86_64-manylinux_2_31 requirements.in -o requirements-cpu.txt`. Expected: langchain-arangodb resolves to >=1.2.0.
- `genie-ai-overlay/contracts/test_contract_label_filter.py` -- Update version assertion at line 150-152 from ==0.0.6 to >=1.2.0. Update assertion message to reflect new minimum version requirement.

**Acceptance Criteria:**
- Given requirements.in has langchain-arangodb>=1.2.0, when pip-compile runs, then requirements-cpu.txt contains langchain-arangodb>=1.2.0 (not ==0.0.6).
- Given langchain-arangodb>=1.2.0 is installed, when the contract test test_installed_arangovector_exposes_filter_clause_named_param runs, then the version assertion passes (not fails with ==0.0.6 check).
- Given langchain-arangodb>=1.2.0 is installed, when ArangoVector.similarity_search and related methods are introspected, then filter_clause appears as a named parameter (not swallowed via **kwargs).
- Given the label-filter exclusion test test_pure_filter_excludes_wrong_category runs, then wrong-category chunks are excluded under both AND and OR strategies.

## Spec Change Log

## Review Triage Log

### 2026-08-13 — Review pass
- intent_gap: 0
- bad_spec: 0
- patch: 1: (low 1) Dockerfile comment stale (references 0.0.6 instead of >=1.2.0)
- defer: 7: (medium 4, low 3) No upper bound on version floor (design decision); no changelog entry (pre-existing); compiled-lock policy no longer stated (design decision); no provenance for 1.2.0 floor (documentation); version floor weakens regression guard beyond named-param check (follow-up: add constructor signature check); dataprep image still pins 0.0.6 (separate concern); uv pip compile command syntax change unexplained (cosmetic)
- reject: 6: Contract test assertion weaker than lock file (intentional floor vs exact pin); packaging.version import inside test function (style nit); InvalidVersion handling (defensive coding for impossible case); packaging module not installed (transitive dep already in lock); other compiled lock files not touched (only cpu variant exists); docstring narrative temporally confused (style improvement)
- addressed_findings:
  - `[low]` `[patch]` Updated Dockerfile comment at lines 40-46 to reflect >=1.2.0 version instead of 0.0.6

## Verification

**Commands:**
- `cd genie-ai-overlay/retriever && uv pip compile --generate-hashes --python-version 3.11 --python-platform x86_64-manylinux_2_31 requirements.in -o requirements-cpu.txt` -- expected: langchain-arangodb resolves to >=1.2.0 in requirements-cpu.txt
- `cd genie-ai-overlay && source .venv/bin/activate && pip install -r retriever/requirements-cpu.txt` -- expected: langchain-arangodb>=1.2.0 installed
- `cd genie-ai-overlay && source .venv/bin/activate && python -m pytest contracts/test_contract_label_filter.py -v` -- expected: all tests pass, including test_installed_arangovector_exposes_filter_clause_named_param with >=1.2.0 assertion

## Auto Run Result

**Summary:** Bumped langchain-arangodb from 0.0.6 to >=1.2.0 in requirements.in, regenerated requirements-cpu.txt with pip-compile (resolved to 1.2.0), and updated the contract test version assertion from ==0.0.6 to >=1.2.0 using packaging.version.Version for semver comparison.

**Files changed:**
- `genie-ai-overlay/retriever/requirements.in` — Updated langchain-arangodb pin from ==0.0.6 to >=1.2.0 with revised comment explaining the fix-pin restoration rationale
- `genie-ai-overlay/retriever/requirements-cpu.txt` — Regenerated compiled lock via pip-compile; langchain-arangodb resolved to 1.2.0 with new hashes
- `genie-ai-overlay/contracts/test_contract_label_filter.py` — Updated version assertion from ==0.0.6 to >=1.2.0 using packaging.version.Version; updated docstring to reflect new minimum version
- `genie-ai-overlay/retriever/Dockerfile-retriever_genie-ai` — Updated stale comment referencing 0.0.6 to reflect >=1.2.0 version

**Review findings breakdown:**
- Patches applied: 1 (Dockerfile comment)
- Items deferred: 7 (design decisions, documentation improvements, separate concerns)
- Items rejected: 6 (intentional design choices, style nits, impossible edge cases)

**Follow-up review recommendation:** false
- Patched findings: 1 low severity
- Score: 0 high + 0 medium + 1 low = 1 (threshold: 3×medium + 1×low ≥ 5)

**Verification performed:**
- pip-compile succeeded: langchain-arangodb==1.2.0 resolved cleanly with no dependency conflicts
- Named-param introspection: installed langchain-arangodb 1.2.0 in test venv and confirmed filter_clause appears as named parameter on all 4 ArangoVector methods (similarity_search, similarity_search_with_score, similarity_search_by_vector, similarity_search_by_vector_with_score)
- Ruff lint: no errors in modified contract test file
- Lock diff minimal: only 4 lines changed in requirements-cpu.txt (version + hashes), no transitive dependency ripple
- In-image contract test execution: not performed (requires full retriever Docker image with vendored comps); assertion logic and named-param surface verified independently

**Residual risks:**
- In-image contract test suite not executed — requires retriever Docker image build; should pass on next CI run
- Version floor (>=1.2.0) allows future major releases; no upper bound to catch breaking API changes in constructor or return formats (deferred: add constructor signature introspection test)
- dataprep image still pins langchain-arangodb==0.0.6 (separate images, separate APIs — not a runtime issue but creates maintenance divergence)
