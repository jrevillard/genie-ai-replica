module gitlab.com/un/itu/genie-ai/site

go 1.26.0

// Docsy is imported via its /theme subpath, which has no release tags.
// This pseudo-version pins main HEAD (requires Hugo >= 0.158.0); bump deliberately.
require github.com/google/docsy/theme v0.0.0-20260626210851-93948e65e6c1 // indirect
