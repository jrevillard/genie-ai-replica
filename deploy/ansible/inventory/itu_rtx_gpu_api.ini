# GPU Node API - ITU RTX test server
# Standalone GPU compose (docker-compose.gpu.yaml) on same host as app stack
#
# Usage:
#   ansible-playbook -i inventory/itu_rtx_gpu_api.ini deploy-gpu.yml --vault-id gpu@prompt

[gpu_nodes]
gpu-api ansible_host=10.0.0.110

[all:vars]
ansible_user=govstack
