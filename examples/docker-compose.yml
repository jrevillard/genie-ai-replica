version: '3.8'

services:
  frontend:
    build: ./gov-chat-frontend
    ports:
      - "8090:8090"
    environment:
      - NODE_ENV=production
    depends_on:
      - backend

  backend:
    build: ./gov-chat-backend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production

