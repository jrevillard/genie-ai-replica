[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --tb=short --junitxml=reports/pytest-report.xml
# The contract suite imports repo code (core/, tracing) and runs from any cwd
# (dev venv or the in-CI contracts/ dir) — keep the overlay root on sys.path.
pythonpath = .
