# Pinned for reproducibility (code-review #14) — the presidio API shifted
# within 2.2.x (OperatorConfig(operator_name, params)); a GDPR gate must be
# byte-reproducible across image builds.
fastapi==0.115.6
uvicorn==0.30.6
pydantic==2.9.2
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
