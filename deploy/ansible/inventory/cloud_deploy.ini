# GENIE.AI Cloud Deployment — bb-ai-vm-02
# Single-node, no local GPU (remote GPU at ai.assembly.govstack.global)

# --- Environment group ---
[cloud_deploy]
genieai-cloud ansible_host=10.0.0.101

# --- Role groups ---
[swarm_managers]
genieai-cloud

# No local GPU — do NOT add to gpu_nodes group

# --- Common variables ---
[all:vars]
ansible_user=govstack
ansible_ssh_private_key_file=/home/fordendk/.ssh/cloud-deploy-np
