# Decision Log — OPEA 1.3 → 1.5 Upgrade (prd_key: opea-1.5-upgrade)

Canonical memory + audit trail. Every decision, change, and override is recorded here as the conversation unfolds.

| # | Date | Decision | Rationale | Recorded in |
|---|------|----------|-----------|-------------|
| 1 | 2026-08-07 | Initiative key: `opea-1.5-upgrade`; PRD worktree `.claude/worktrees/prd` on branch `feat/opea-1.5-upgrade/prd` (base `main`). | User directive; BMAD branch convention `feat/{prd_key}/prd`. | activation |
| 2 | 2026-08-07 | **Primary motivation = upstream improvements + bug fixes + CVE remediation.** Agentic adoption is NOT the driver (contrary to the team-briefing framing). | User directive: "goal of the update is to benefit from the improvements and bug fixes (also solve a couple of CVEs…)". PRD framing centers reliability/security/staying current. | Discovery |
| 3 | 2026-08-07 | No enumerated CVE list; "updating the libraries will fix some of them for sure". Agentic is **out of this PRD** (context only). Working mode = **Fast path**. | User directives. | Discovery |
| 4 | 2026-08-07 | Scope confirmed (S1–S5 in, D1–D4 done, O1 out): modules as listed; Python 3.11; 1.5 pins + langchain-arangodb bump; vLLM pin in; latent dataprep bugs in; smoke tests; RAG parity vs v1.3; CVE evidence; el-salvador canary; agentic/OKF/SST out. Full PRD drafted (Fast path). | User: "all good, draft it". | Draft |
| 5 | 2026-08-07 | **Party-mode review (Winston/John/Amelia/Murat) folded into the PRD:** kwargs-forwarding spike → blocking pre-rebase milestone + orchestrator wire test; baseline capture → pre-rebase milestone; FR-10 upgraded import→contract tests (isolated from mocked conftest, red-green validated); FR-12 CVE gate → baseline-diff + "no net-new high/critical"; FR-15 extended to embedding/textgen bases (split-brain) + separate commit; FR-16 explicit canary exit criteria + shadow option; FR-17 rollback → rehearsed + data-level backward-read + v1.3 retention; FR-2 "bit-identical"→"within tolerance"; FR-6 surfaces expanded (dataprep `_parent_mod` target, kwargs 2nd hop, orchestrator ctor, integration discovery, Pydantic semantics); FR-19 added (targeted improvements positive check); §6.1 sequencing (baseline→spike→cleanup-commit→tests-green, per-module bump); SM-3 revised, SM-7/SM-C4 added; §10/§11 updated. | User: "fold them into the PRD". | Reviewer-gate (ad-hoc) |
| 6 | 2026-08-07 | **Formal reviewer pass (rubric walker):** verdict **strong / approvable** — 0 critical, 1 high, 4 medium, 3 low. **High fixed:** translation scope clarified (rides chatqna rebase; `vllm-translation-guardrail` already pinned; FR-15 + §9). **Mechanical fixed:** §0 cross-refs (§10→§11, "referenced in §9"), FR-19 moved to §4.5 in numeric order (§4.5 heading restored). **Deferred (non-blockers, owners logged):** no consolidated risk register/effort estimate at launch approval; deferred acceptance values (FR-16 canary thresholds, FR-19 enumeration, OQ-2); 10/19 FRs lack a validating SM; NFR-R1 "under load" unanchored; "8-kwargs" vs "six custom kwargs" drift; UJ-2 protagonist unnamed. | Reviewer pass (launch stakes). | Reviewer-gate (formal) |
| 7 | 2026-08-07 | **Polish (editorial structure + prose passes) applied:** FR-6 enumeration → bullets; §11 Assumptions Index reconciled to actual inline `[ASSUMPTION]` tags only (removed self-negating CVE entry + verified-fact v1.5 entry); review-process meta ("after review"/"per review") stripped; OQ1 condensed; §1 "7.5 months drift" figure clarified; §4.4 "gates gate" fixed. **PRD marked `status: final`.** | Finalize polish step. | Finalize |
| 8 | 2026-08-07 | **Input reconciliation (Finalize step 2):** review input → no gaps (C1–C11, G1–G5 all covered). deferred-work input → 3 material + 2 low gaps folded in: dataprep Dockerfile `REQ_PATH` rewrite + sed re-audit + `opencv-python-headless` re-confirm (FR-9); `Makefile` lock targets re-pointed (FR-4); `openai-whisper` restore-vs-drop + retriever/reranker compiled-lock adoption added as Open Questions 9–10. No conflicts. | Reconciliation subagent. | Finalize |
| 9 | 2026-08-07 | **on_complete executed:** GitLab issue **#837** "PRD: opea-1.5-upgrade" (labels `type::prd`, `prd::opea-1.5-upgrade`) created; branch committed + pushed (`update PRD opea-1.5-upgrade`); draft MR **!277** `feat/opea-1.5-upgrade/prd` → `main` created (draft set via title prefix). Full BMAD PRD process closed. | complete.yaml. | Close |
| 10 | 2026-08-07 | **Architecture complete** (`_bmad-output/planning-artifacts/architecture.md`, committed `update architecture`): D1–D8 decisions (cleanest-architecture principle), 12 implementation patterns + `verify:evidence`, artifact-lifecycle policy (keep verification infra, ledger transient), structure, validation = READY FOR IMPLEMENTATION. | bmad-create-architecture workflow (steps 1–8, incl. party-mode rounds). | Architecture |
| 11 | 2026-08-07 | **PRD reconciled to architecture:** FR-4 + SM-6 compiled-lock extended to retriever/reranker (D7); FR-10 contract tests in-image required (D3) + E2E cross-service pipeline test + NFR-P budgets (D6); FR-16 shadow comparison required (D5); FR-18 docs-link bound to the OPEA_VERSION MR (pattern 9); FR-6 override-audit manifest; OQ-9 resolved (D8), OQ-10 resolved (D7). | PRD amendment (validation important-gap). | Reconciliation |

| 12 | 2026-08-07 | **Epics + stories complete** (`_bmad-output/planning-artifacts/epics.md`): 4 epics (foundation / overlay rebase / verification & parity / operational readiness & rollout), 23 stories, all 19 FRs covered, final validation passed (single-agent stories, prior-only deps, justified file-churn split). | bmad-create-epics-and-stories workflow (steps 1–4). | Epics |

| 13 | 2026-08-12 | **FR-4 executed (story 2-2): `verify:dataprep-lock` re-pointed to three-module compiled-lock drift guard.** Job renamed in scope only (kept name `verify:dataprep-lock`), now loops `dataprep`/`retriever`/`reranker` running `uv pip sync requirements-cpu.txt --dry-run --system` + per-package grep of each `requirements.in` against its `requirements-cpu.txt` (`make lock-<module>` hint on miss). Image `python:3.10-slim`→`python:3.11-slim`; rules paths widened to `genie-ai-overlay/{dataprep,retriever,reranker}/requirements.*` (retired `requirements.lock`, `dataprep/scripts/*` refs). Matches new compiled-lock layout + AC5. | PRD FR-4 + SM-6; upstream v1.5 lock (no hashes) drove adoption of uv-compiled hashed locks. | Implementation (2-2) |
