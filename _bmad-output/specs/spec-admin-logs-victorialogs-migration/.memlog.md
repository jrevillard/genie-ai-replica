---
topic: Admin Logs + Security-Scan migration from Winston files to VictoriaLogs (P0-P4)
updated: 2026-08-31T17:44
---

- (direction) Mandate from D. Forden (2026-08-15): the Winston logger must write directly to VictoriaLogs — not via console and not via indirect fluentd-only chain.
- (decision) D1: split victorialogs + otel-collector out of profiles:[observability] into profiles:[core] in docker-compose.yaml (lines 1650, 1671, 1749) — VL+Collector always run with stack; admin endpoints always functional.
- (decision) D3: VL service filter scope = all services. Default filter in admin UI = All. Full-stack view enabled.
- (decision) D4: producer-first sequencing — Winston emits to VL before any consumer rewires land. Supersedes earlier 'structured JSON later' plan.
- (decision) Winston format switches from printf to JSON now — the printf constraint existed only because admin regex parsers depended on it. With VL transport (structured LogRecord attributes) and JSON file fallback, the constraint disappears. Fixes F4 (broken triple-bracket regex at admin-dashboard-service.js:525) at root.
- (capability) CAP-1 Producer emits structured log records to VictoriaLogs — Winston logger writes via LoggerProvider + OTLPLogExporter + BatchLogRecordProcessor; gated on LOG_TO_VICTORIALOGS=1 AND ENABLE_OBSERVABILITY=1; success: POST to otel-collector:4318/v1/logs visible in VL /select/logsql/query?q=service:genie-backend within 5s; killing VL does not block services.
- (capability) CAP-2 Winston format is JSON (winston.format.combine(timestamp(), errors({stack:true}), json())) — replaces printf + traceFormat at logger.js:24-30; trace_id/span_id become JSON keys; success: file lines are valid JSON one record per line when LOG_TO_FILE=1; logSearchDialog.test.js Story 7.6 describe block parses JSON instead of regex.
- (capability) CAP-3 Admin Logs endpoints query VictoriaLogs — GET /api/admin/logs, /summary, /search, /debug-yesterday; LogsService rewired to VictoriaLogsClient; F4 regex at admin-dashboard-service.js:525 removed; success: contract tests assert response shapes identical pre/post migration (deep-equal against same fixture). admin-routes.js:141/374/435/464.
- (capability) CAP-4 Security scanner queries VictoriaLogs — POST /api/admin/security-scan drops worker_threads and streams LogSQL _msg OR-joined needles to VL /select/logsql/query; 14 vulnerability patterns preserved (security-scan-service.js:111-226); success: scan over 7-day window completes in &lt;2s; response shape vulnerabilities.{critical,medium,low}[] unchanged; /app/data/security/last-scan-results.json cache write retained.
- (capability) CAP-5 Graceful degradation when VictoriaLogs unreachable — VL_FAIL_OPEN=true env returns empty results + degraded:true flag; default false lets 5xx surface to admin; success: docker stop victorialogs + VL_FAIL_OPEN=true + GET /api/admin/logs returns {logs:[], total:0, degraded:true} AND backend logger.error fires at most once per minute rate-limited.
- (capability) CAP-6 Per-phase rollback escape hatches — LOG_TO_VICTORIALOGS=0 (P1a, restart), ADMIN_LOGS_SOURCE=file (P2, no restart), VL_FAIL_OPEN=true (P3, no restart), LOG_TO_FILE=1 (P4, restart); each switch tested with smoke on the deployed release branch before merge to main (per feedback_release_validate_before_promote).
- (capability) CAP-7 VL + OTel Collector always-on core stack — D1 (a) split; compose profiles:[core] on otel-collector-init, otel-collector, victorialogs (docker-compose.yaml:1650,1671,1749); non-Node (Python OPEA, Kong, nginx, postgres) remain on fluentd→collector→VL; success: docker compose --profile core up -d brings VL+Collector without --profile observability; curl http://victorialogs:9428/health returns {status:ok}.
- (capability) CAP-8 CI gap fix — .gitlab-ci.yml:2942-2984 references tests/melt-correlation/run-melt-test.sh but directory absent; add stub exit-0 + README in P0 MR; keep scheduled:melt-correlation job allow_failure:true until full impl lands in follow-up epic.
- (constraint) Backend JS = CommonJS only (no ES imports) — per project-context.md rule 1. Winston TransportStream subclass + VictoriaLogsClient use require()/module.exports.
- (constraint) Response shape contract preservation — LogsService public methods getLogs/getLogsSummary/searchLogs/getDebugYesterday and securityScanService.runSecurityScan return JSON identical to pre-migration. Contract tests (logs-vl-contract.test.js, security-scan-vl-bulk.test.js) capture both responses from same fixture and assert deep-equal. Gate MR.
- (constraint) VL retention 30d aligned with existing Winston DailyRotateFile 30d — env.j2:229 already renders VICTORIALOGS_RETENTION={{ victorialogs_retention | default('30d') }}; no change to retention period.
- (constraint) VL stream fields pinned to service.name + deployment.environment — trace_id/span_id are attributes NOT stream fields (cardinality control per study §9 risk row 7).
- (constraint) PII scrubbing extended to OTel LogRecord attributes — never log raw tokens, passwords, or user PII in span/log attributes (per tracing-obbing-tracing.md rule + existing PIIRedactionProcessor at gov-chat-backend/tracing.js:52-96,160).
- (constraint) Per CLAUDE.md git workflow: never commit to main or release/* directly. Worktree + dedicated branch feat/admin-logs-victorialogs + one MR per phase (P0/P1a/P1b/P1c/P2/P3/P4). Per feedback_never_merge_without_ci: wait for pipeline green before merge.
- (non-goal) okf-server migration — does not exist in repo (study §1 wrong on third Node service). Only gov-chat-backend + document-repository are Node services. Plan covers backend (producer+consumer) + document-repository (producer only — no admin endpoints there).
- (non-goal) Frontend AdminDashboard.vue / LogSearchDialog.vue contract changes — response shapes preserved by contract tests; UI gains degraded:true banner via existing mechanism, no Vue rewrites. Vue 3 Options API preserved per project-context.md.
- (non-goal) Grafana service-logs dashboard rewrites — derived fields TraceID/SpanID at vm-datasource.yml:13-21 already work with OTel logs; new structured level/service.name fields extract natively. Dashboard variables stay text-match.
- (non-goal) Full tests/melt-correlation/ implementation — P0 MR adds exit-0 stub to unblock CI; full chaos/correlation suite is a separate epic.
- (non-goal) document-repository admin endpoints — none exist. Producer-only changes land in document-repository/src/app.js (no OTel SDK today; minimal tracing.js-equivalent added in P1a follow-up).
- (assumption) Assumed VL is reachable in-network from backend + document-repository via DNS name victorialogs:9428 — confirmed by docker-compose.yaml:1749 service name + 1758 vlogs-data volume.
- (assumption) Assumed no other consumer of admin-dashboard-service.js public methods (getLogs/rolloverLogs/getLogsSummary) besides routes — requires grep verification in MR-5 description.
- (question) Open: do we want @opentelemetry/sdk-logs deps added to components/shared/lib/package.json (shared) or only to components/gov-chat-backend/package.json (backend-only) and document-repository's own? Plan says both; component-boundary rule from project-context.md prefers deps live with consumer. Confirm before P1a MR.
- (question) Open: confirm CAP-6 (rate-limited error logging at 1/min) — is 1/min the right cadence for VL outage observability or should it be lower (e.g., 1 per 5min) to avoid log flood during extended outages?
- (event) Self-validate Pass 1 (Coherence): 8 capabilities all carry intent+success. Constraints bend design (CommonJS rules out ESM, contract parity gates MRs, stream-field cardinality controls log growth, git workflow rules out direct main commits). Non-goals explicit (5 items). Success signal concrete. Verdict: pass.
- (event) Self-validate Pass 2 (Preservation): all load-bearing claims from the technical plan and the 2026-08-15 study land in SPEC.md kernel or companions (phases.md, env-vars.md, vuln-patterns.md, rollback-matrix.md). Wrapper-only drops: study prose intro/scope rationale (preserved as event entries); section numbering; per-phase effort estimates (kept in phases.md). Verdict: pass.
- (decision) D2: ADMIN_LOGS_SOURCE=file|victorialogs env switch is a permanent escape hatch — never removed. Default flips from file to victorialogs after first release.
- (decision) D6: app OTLP logs egress via OTel Collector at :4318/v1/logs — reuses Collector's sending-queue/retry config. Direct VL is allowed only as env override.
- (event) Review pass (bmad-review, 64 findings across 4 lenses: adversarial=16, edge-case-hunter=31, structure=12, prose=5). Applied high-impact + structural fixes. SPEC.md: Why split into 4 sentences, CAP-6 behavior, CAP-8 deleted (folded into NG-4), new ## Decisions subsection with D1/D2/D3/D4/D6, Success signal pointer to verification.md, A-2 MR-5 wording dropped, Q-2 standardized, CAP-5 extended to security-scan + ECONNREFUSED/ENOTFOUND + rate-limit persistence. env-vars.md: 30d spacing fix, env file layout block replaced, added VICTORIALOGS_TENANT_ID + VL_QUERY_TIMEOUT_MS, observability_disabled counter. phases.md: P1a lazy-init ring buffer + boolean coercion + OTel setGlobalLoggerProvider + Prometheus log_record_dropped_total + 5s indexing lag banner + PII body field. P1b VL 1.50+ AccountID/ProjectID, axios timeout env, 3x5s health probe, drop empty trace_id, LogSQL reserved chars escape, MELT_PROVIDER whitelist. P1c namespace note + dual-emit dedup filter + fluentd anchor lint + smoke shell script + full anchor+service rollback. P2 fixture file + NDJSON schema + ingestion script + per-call env read + JSON.parse try/catch + error.stack newline guard + ENOENT tolerance + O_EXCL + rolloverLogs 410 Gone + banner scope + grep other printf. P3 dedupe by record key + truncation guard + retention check + cache schema validate + security-scan-vl-degradation + delete gzip/Worker. P4 triggerLogRollover internal import + linter rule + dual-write volume note + CI allow_failure + docker exec du. Worktree: merge order gate. rollback-matrix: P1b row removed + P1c full anchor rollback. verification: Keycloak citation + banner check + security-scan degradation + docker exec du. vuln-patterns: dedupe + truncation notes. PENDING: Q-1 dep location, Q-2 rate-limit cadence.
- (decision) Q-1 RESOLVED: OTel deps split per option C. shared/lib/victorialogs-transport.js requires only @opentelemetry/api-logs (thin wrapper). Heavy @opentelemetry/sdk-logs + @opentelemetry/exporter-logs-otlp-http live per-component (gov-chat-backend + document-repository). shared/lib stays dep-light; each component controls its own init (resource attributes, BatchLogRecordProcessor config per AD-18, PII processor chain). Applied to env-vars.md dependency table.
- (decision) Q-2 RESOLVED: VL_FAIL_OPEN rate-limit cadence = 1 per minute (as drafted in CAP-5 + AD-11). State persisted to /tmp/vl-fail-open-ts with Unix ms + O_EXCL claim.
- (decision) Q-4 RESOLVED: log_record_dropped_total carries 3 reason labels (queue_full, otlp_unreachable, observability_disabled) ONLY. NO per-service label — Prometheus scrape instance label already gives per-process visibility; adding service multiplies cardinality without diagnostic gain. Per-service aggregation available via sum by (instance) (rate(...)).
